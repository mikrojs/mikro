# mikrojs

Deprecated. The `mikrojs` package was renamed to [`mikro`](https://www.npmjs.com/package/mikro). Install `mikro` and use the `mikro` command instead. This package only prints a notice and exits.
