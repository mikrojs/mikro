{
  "name": "@mikrojs/firmware-dev",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Mikro.js firmware dev project",
  "scripts": {
    "build": "mikro idf build",
    "flash": "mikro idf build flash monitor",
    "test": "cd test && mikro idf -B build build partition-table partition-table-flash flash monitor",
    "test:protocol": "npx tsx test-protocol.ts"
  },
  "dependencies": {
    "@mikrojs/firmware": "workspace:*",
    "mikro": "workspace:*"
  }
}
